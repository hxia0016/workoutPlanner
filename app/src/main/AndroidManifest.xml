<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.workoutplanner">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorkOutPlanner">
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.WorkOutPlanner.NoActionBar"
            android:exported="true"
            android:windowSoftInputMode="adjustNothing|stateHidden"/> //do not effected by the keyboard
        <activity
            android:name=".UserSignIn.ForgetPassword"
            android:exported="false"
            android:windowSoftInputMode="adjustNothing|stateHidden"/>
        <activity
            android:name=".UserSignIn.RegisterUser"
            android:exported="false"
            android:windowSoftInputMode="adjustNothing|stateHidden"/>
            <activity
                android:name=".UserSignIn.LoginUser"
                android:exported="true"
                android:windowSoftInputMode="adjustNothing|stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>